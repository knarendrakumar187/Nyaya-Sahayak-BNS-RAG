# Optional override. Default in code is already:
# https://nyaya-sahayak-api.onrender.com
#
# On Vercel you can set:
# VITE_API_BASE_URL=https://nyaya-sahayak-api.onrender.com
#
# IMPORTANT: Do NOT rely on same-origin /api if Vercel Deployment Protection
# (SSO) is on — it redirects /api and causes "Failed to fetch".
VITE_API_BASE_URL=https://nyaya-sahayak-api.onrender.com
